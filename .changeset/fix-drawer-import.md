---
"rivet-ui": patch
---

Align with `@base-ui/react@1.4.1`. The Drawer namespace export graduated from preview (`DrawerPreview` → `Drawer`) in 1.3.0; the source already used the new name, so this just bumps the locked peer.
